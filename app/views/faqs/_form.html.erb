<%= form_for(@faq) do |f| %>
  <% if @faq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@faq.errors.count, "error") %> prohibited this FAQ from being saved:</h2>

      <ul>
      <% @faq.errors.full_messages.each do |message| %>
	<li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
	  <%= f.label :question %><br />
	  <%= f.text_field :question %>
  </div>
  <br />

  <div class="field">
	  <%= f.label :answer %><br />
	  <%= f.text_area :answer, rows: "3"  %>
  </div>

  <br />

  <div class="actions">
    <%= f.submit  class: "btn btn-primary", reload: true %>
  </div>
<% end %>
